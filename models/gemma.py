def run_gemma_inference(prompt: str) -> str:
    # Stubbed model logic - replace with real Gemma inference call
    if "panic" in prompt.lower():
        return "The message indicates high panic and may need urgent attention."
    else:
        return "The message is calm and controlled."
